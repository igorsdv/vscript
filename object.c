#include "main.h"

/*Object object_new(ObjectType type, byte *value, int size)
{
	Object obj;
	obj.type = type;
	obj.value = value;
	obj.size = size;
	obj.refcount = 0;
	return obj;
}

void object_free(Object obj)
{
	free(obj.value);
}*/
